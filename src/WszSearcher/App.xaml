<Application x:Class="WszSearcher.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:tb="http://www.hardcodet.net/taskbar"
             Exit="OnAppExit">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Styles/Theme.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!-- 系统托盘图标 -->
            <tb:TaskbarIcon x:Key="TrayIcon"
                            IconSource="/Resources/app.ico"
                            ToolTipText="{DynamicResource Lang.Tray.Tooltip}"
                            Visibility="Visible"
                            TrayLeftMouseUp="OnTrayLeftClick">
                <tb:TaskbarIcon.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="{DynamicResource Lang.Tray.ShowWindow}" Click="OnShowWindow" />
                        <MenuItem Header="{DynamicResource Lang.Tray.Settings}" Click="OnOpenSettings" />
                        <Separator />
                        <MenuItem Header="{DynamicResource Lang.Tray.About}" Click="OnShowAbout" />
                        <MenuItem Header="{DynamicResource Lang.Tray.Exit}" Click="OnExitApp" />
                    </ContextMenu>
                </tb:TaskbarIcon.ContextMenu>
            </tb:TaskbarIcon>
        </ResourceDictionary>
    </Application.Resources>
</Application>
